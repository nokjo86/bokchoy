<% if flash[:alert] %>
  <p class="alert alert-danger"><%= flash[:alert] %></p>
<% end %>
<div class="container">
  <div class="listings">
    <% @listings.each do |listing| %>
      <%= render 'shared/card', listing: listing %>
    <% end %>
  </div>
</div>