<div class="container">
  <h1>Shopping basket</h1>
  <% if @cart.length > 0 %>
  <%= render "cart_table", cart: @cart %>
  <div>
    <input type="radio" name="delivery" id="pickup" value=1 checked >
    <label for="pickup">Local Pickup</label>
    <input type="radio" name="delivery" id="delivery" value=2>
    <label for="delivery">Delivery</label><br>
  </div>
    <%# <% hidden_field_tag 'seller', item.profile.id %>
  <div>
    <%= link_to "Pay Now", "", id: "stripe", class: "btn btn-success float-right" %>  
  </div>
  <% else %>
  <%= "Your veggie box is currently empty, start shopping now" %>
  <% end %>
</div>

<% item = @cart.first %>

<%= javascript_pack_tag "stripe" %>